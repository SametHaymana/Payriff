{
  "name": "payriff",
  "version": "1.1.2",
  "description": "The payment system mostly used in Azerbaijan is payriff. This package is an unofficial sdk of payriff payment system",
  "author": "",
  "main": "index.js",
  "types":"types",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "npm run build",
    "version": "git add -A src",
    "postversion": "git push && git push --tags"
  },
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.4"
  },
  "devDependencies": {
    "typescript": "^4.9.5"
  }
}
